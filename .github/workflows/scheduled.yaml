name: Cron Job

on:
   schedule:
     - cron: "0 5 * * 6"
jobs:
    daily-task:
        runs-on: ubuntu-latest
        steps:
          - name: Print Message
            run: echo "Running the Scheduled task"