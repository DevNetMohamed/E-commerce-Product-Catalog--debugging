name: CI Pipeline

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run Test
        run: echo "test"

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Build Project
        run: echo "build"
